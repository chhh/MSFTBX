rootProject.name = 'batmass-io'

// Alternative to setting a dependency on a pusblished artifact.
// When file `.composite-enable` is present, will use the project for
// build instead of the downloaded jar.
//if (file("./.composite-enable").exists()) {
//    includeBuild ('../lib-javolution-msftbx')
//}
